---
interface Props {
	image?: string;
	alt?: string;
}

const { image, alt } = Astro.props;
---
<div>
    <img 
        class="!w-full !max-w-none lg:h-[30vh] h-[20vh] object-cover" 
        src={image}
        alt={alt}
    />
</div>
